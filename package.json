{
  "name": "grok-mcp",
  "version": "0.4.0",
  "description": "MCP server that delegates coding tasks to Grok Build (headless) as a subagent",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test:cli": "node scripts/cli-regression.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0"
  }
}
